<% unless correct_user?(@user) %>
  <div id="follow_form">
    <% if current_user.following?(@user) %>
      <%= link_to t("unfollow"), "#", class: "btn btn-default" %>
    <% else %>
      <%= link_to t("follow"), "#", class: "btn btn-primary" %>
    <% end %>
  </div>
<% end %>
